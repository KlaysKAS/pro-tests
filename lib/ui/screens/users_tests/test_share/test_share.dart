import 'package:flutter/material.dart';

class TestShareScreen extends StatelessWidget {
  const TestShareScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}

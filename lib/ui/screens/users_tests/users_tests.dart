import 'package:flutter/material.dart';

class UsersTestsScreen extends StatelessWidget {
  const UsersTestsScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Placeholder(),
    );
  }
}

import 'package:flutter/material.dart';

class TestAddingScreen extends StatelessWidget {
  const TestAddingScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
